import { products } from "../products";

const endpoints ={

    auth:{
        login:"auth/login",
        signup:"auth/signup",
    },
    products:{
        list:products,

    }

}
export default endpoints;   